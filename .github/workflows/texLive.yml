name: Latex_Action

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: xu-cheng/latex-action@v3
  with:
    root_file: |
      2024/cv.tex